# dependencias
node_modules/

# artefactos de build (binario SEA ~124MB, tarball, bundle CJS, build de la web)
dist/
.build/
.build-win/

# datos locales del vault — NUNCA commitear: contienen la llave maestra en claro
.dotrino/
*.tmp

# OS / editores / agente
.DS_Store
.claude/
usage-stats.json
# El lockfile de lib/: ahí solo hay peer y dev deps (las de verdad las fija el repo raíz)
lib/package-lock.json

# tokens de publicación a npm — NUNCA commitear
**/.npmrc
